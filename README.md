# streaming-pricing

Run application:
```mvn spring-boot:run```

Run automation test:
```mvn test```

Implement data structure with Hashmap as below:
```Map<identifier, TreeMap<timestamp, observation>>```
